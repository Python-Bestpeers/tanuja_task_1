from django.urls import path
from .views import (
    LoginView,
    RegistrationView,
    HomePage,
    LogoutPage,
    ProfileView,
    TaskCreateView,
    TaskView,
    TaskDetails,
    CommentView,
    DeleteTask,
    TaskUpdateView,
    CommentShow,
    UserCreate,
    UserList,
    TaskSearch,
)

urlpatterns = [
    path("", LoginView.as_view(), name="loginform"),
    path("signup-form/", RegistrationView.as_view(), name="signupform"),
    path("home-page/", HomePage.as_view(), name="homepage"),
    # path('userhome/',userhome.as_view(),name="userhome"),
    path("logout-page/", LogoutPage.as_view(), name="logoutpage"),
    path("profile-view/", ProfileView.as_view(), name="profileview"),
    path("task-create/", TaskCreateView.as_view(), name="taskcreate"),
    path("task-view/", TaskView.as_view(), name="taskview"),
    path("task-details/<int:id>", TaskDetails.as_view(), name="taskdetails"),
    path("delete-task/<int:id>", DeleteTask.as_view(), name="deletetask"),
    path("comment-data/<int:id>", CommentView.as_view(), name="commentdata"),
    path("comment-show/<int:id>", CommentShow.as_view(), name="commentshow"),
    path("update-task/<int:id>", TaskUpdateView.as_view(), name="updatetask"),
    path("user-create/", UserCreate.as_view(), name="usercreate"),
    path("user-List/", UserList.as_view(), name="userlist"),
    path("task-search/", TaskSearch.as_view(), name="tasksearch"),
]
